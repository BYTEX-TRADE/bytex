# Tests

Test projects live here: unit tests per package (Bytex.Core.Tests, ...),
integration tests against venue testnets, and BenchmarkDotNet performance
benchmarks. Package versions for xunit, coverlet, and BenchmarkDotNet are
pinned in Directory.Packages.props.
